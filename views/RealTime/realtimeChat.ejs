<!-- Wappler include head-page="layouts/main" fontawesome_5="cdn" bootstrap5="local" is="dmx-app" id="realtimeChat" appConnect="local" -->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat com OpenAI</title>
    <!-- Bootstrap CSS CDN -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" crossorigin="anonymous">
    <style>
        /* Estilo para o contêiner de mensagens */
        #messages {
            background-color: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 8px;
            padding: 15px;
            overflow-y: auto;
            height: calc(50vh - 100px);
            /* Altura dinâmica para texto */
            margin-bottom: 10px;
        }

        /* Estilo para o contêiner de áudio */
        #audio-messages {
            background-color: #e9ecef;
            border: 1px solid #dee2e6;
            border-radius: 8px;
            padding: 15px;
            overflow-y: auto;
            height: calc(30vh - 50px);
            /* Altura dinâmica para áudio */
        }

        /* Estilo das mensagens */
        .message {
            margin-bottom: 15px;
            padding: 10px;
            border-radius: 8px;
            max-width: 70%;
            word-wrap: break-word;
        }

        .message.bot {
            background-color: #e9ecef;
            align-self: flex-start;
        }

        .message.user {
            background-color: #d1e7dd;
            align-self: flex-end;
            text-align: right;
        }
    </style>
</head>

<body>
    <div class="container d-flex flex-column vh-100">
        <!-- Header -->
        <header class="py-3 text-center">
            <h1 class="mb-0">Chat com OpenAI</h1>
            <p class="text-muted small">Texto e Áudio em Tempo Real</p>
        </header>

        <!-- Chat Container -->
        <main id="messages" class="d-flex flex-column">
            <!-- Mensagens de texto exibidas aqui -->
        </main>

        <!-- Audio Container -->
        <section id="audio-messages" class="audio-container">
            <!-- Mensagens de áudio exibidas aqui -->
        </section>



        <!-- Input Container -->
        <footer class="d-flex align-items-center gap-2">
            <input type="text" id="userInput" class="form-control" placeholder="Digite sua mensagem...">
            <button class="btn btn-primary" onclick="sendMessage()">Enviar</button>
            <button class="btn btn-secondary" onclick="startRecording()" id="recordButton">🎤 Gravar</button>
        </footer>
    </div>

    <!-- Bootstrap JS CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>

    <!-- Script -->
    <script src="/assets/js/chat%20-%20audio%20e%20texto.js"></script>
</body>

</html>